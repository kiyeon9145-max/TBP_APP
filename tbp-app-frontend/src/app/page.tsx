export default function Home() {
  return (
    <main style={{ padding: 'var(--space-4)' }}>
      <h1 style={{ fontSize: 'var(--font-title)', fontWeight: 'var(--font-title-weight)' }}>
        PDS Frontend Foundation
      </h1>
      <p style={{ marginTop: 'var(--space-4)', color: 'var(--color-text-secondary)' }}>
        Foundation 구축 완료. 다음 STEP 5에서 Design System과 Common Components를 구현합니다.
      </p>
    </main>
  );
}
