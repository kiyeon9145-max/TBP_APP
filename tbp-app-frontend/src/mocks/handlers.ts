import type { HttpHandler } from 'msw';

// TODO: Backend API 계약 확정 후 핸들러 작성
// 예시:
// import { http } from 'msw';
// export const handlers: HttpHandler[] = [
//   http.get('/api/players', () => {...}),
//   http.post('/api/players', () => {...}),
// ];

export const handlers: HttpHandler[] = [];
